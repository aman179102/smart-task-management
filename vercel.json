{
  "framework": "vite",
  "buildCommand": "pnpm --prefix client install --frozen-lockfile && pnpm --prefix client build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
